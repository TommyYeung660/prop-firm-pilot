# v1.4.8 Report Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 產出 `docs/PropFirmPilot_v1.4.8_Report.md`，用 12 個高真實度數字情景說明 `v1.4.8` close control plane 的完整交易流程、tactical operations、audit trail、可持續學習性，以及對 `v1.4.9` / `v1.5.0` 的基線價值。

**Architecture:** 報告沿用 `v1.4.7` 的 `Part A / B / C / D` 結構，但把 close-domain 語義寫得更細。Part A 定位文件角色與邊界，Part B 描述標準主流程，Part C 用 12 個定量情景拆解 tactical operations，Part D 收斂為 learning loop、audit trail 與 roadmap baseline。所有 close semantics 均對齊 `CloseControlPlane`、`CloseReconciler` 與 `Scheduler` 的當前實作。

**Tech Stack:** Markdown, existing `docs/PropFirmPilot_v1.4.7_Report.md`, `src/decision/close_control_plane.py`, `src/decision/close_reconciler.py`, `src/scheduler/scheduler.py`, `git diff --check`

---

### Task 1: Freeze Report Outline And Constraints

**Files:**
- Create: `docs/plans/2026-03-15-v1.4.8-report-design.md`
- Create: `docs/plans/2026-03-15-v1.4.8-report.md`
- Reference: `docs/PropFirmPilot_v1.4.7_Report.md`

**Step 1: Confirm structural baseline**

Lock the report to:

- `Part A — 報告定位`
- `Part B — Close Control Plane 標準主流程`
- `Part C — 12 個細顆粒交易情景`
- `Part D — 可持續學習 / Audit Trail / 後續缺口`

**Step 2: Lock terminology rule**

Require that each professional term is followed by a brief Chinese explanation on first appearance.

**Step 3: Lock scenario field schema**

Ensure all 12 scenarios will include fixed numeric operational fields and canonical close fields.

**Step 4: Save the design constraints**

Persist the design in `docs/plans/2026-03-15-v1.4.8-report-design.md`.

### Task 2: Draft Part A And Part B

**Files:**
- Create: `docs/PropFirmPilot_v1.4.8_Report.md`
- Reference: `docs/PropFirmPilot_v1.4.7_Report.md`
- Reference: `src/decision/close_control_plane.py`
- Reference: `src/decision/close_reconciler.py`
- Reference: `src/scheduler/scheduler.py`

**Step 1: Write report header and TOC**

Mirror the `v1.4.7` report style:

- title
- report date
- version
- baseline version
- focus
- TOC with numbered sections

**Step 2: Write Part A**

Cover:

- report purpose
- why this is a rule-driven operational baseline, not live validation
- how `v1.4.8` extends `v1.4.7`
- role split between scanner, `TradingAgents`, and close control plane

**Step 3: Write Part B**

Describe the standard flow from:

- scanner
- `TradingAgents`
- opened position
- tactical entry context
- close trigger creation
- control plane execution
- reconciler finalization
- journal and memory output

**Step 4: Check terminology consistency**

Make sure first-use explanations are present for close-domain terms.

### Task 3: Draft Scenarios 1-6

**Files:**
- Modify: `docs/PropFirmPilot_v1.4.8_Report.md`

**Step 1: Write scenarios 1-3**

Cover:

- tactical partial close then TP
- breakeven then SL
- multi-step trailing stop protection

Each scenario must include concrete prices, ATR, realized volatility, spread, action sequence, and final reconciliation.

**Step 2: Write scenarios 4-6**

Cover:

- TP repricing
- tactical full exit
- modify verify mismatch then original protection hit

Each scenario must clearly separate:

- broker write state
- readback state
- final close reason

**Step 3: Verify internal consistency**

Check that price path, SL/TP logic, and PnL math are coherent enough for operator reading.

### Task 4: Draft Scenarios 7-12

**Files:**
- Modify: `docs/PropFirmPilot_v1.4.8_Report.md`

**Step 1: Write scenarios 7-9**

Cover:

- drawdown `reduce_exposure`
- `emergency_close`
- `reeval_close`

**Step 2: Write scenarios 10-12**

Cover:

- `best_day_close`
- `manual_or_broker`
- `execution_meta + last_known_profit`

**Step 3: Check canonical field usage**

Every scenario must explicitly state:

- `trigger_source`
- `action_kind`
- `final_close_reason`
- `resolution_path`

### Task 5: Draft Part D

**Files:**
- Modify: `docs/PropFirmPilot_v1.4.8_Report.md`

**Step 1: Write sustainable learning section**

Explain how `CLOSE_CONTROL_EVENT` and `TRADE_CLOSED` become learning substrate for reflection, operator review, and tuning.

**Step 2: Write audit trail section**

Show the replay path of one trade from trigger to reconciliation.

**Step 3: Write roadmap baseline sections**

Separate:

- `v1.4.9` bugfix / micro-tuning baseline
- `v1.5.0` architectural baseline

**Step 4: Write glossary**

Add a concise `術語速覽` section for close-domain fields and operations.

### Task 6: Proofread And Verify

**Files:**
- Modify: `docs/PropFirmPilot_v1.4.8_Report.md`

**Step 1: Read through for tone and boundary discipline**

Ensure the report does not overclaim live validation and does not read like a changelog.

**Step 2: Check markdown and whitespace**

Run:

```bash
git diff --check -- docs/plans/2026-03-15-v1.4.8-report-design.md docs/plans/2026-03-15-v1.4.8-report.md docs/PropFirmPilot_v1.4.8_Report.md
```

Expected: no whitespace or conflict-marker errors

**Step 3: Review working tree**

Run:

```bash
git status --short
```

Expected: only the new report-related docs plus the pre-existing unrelated untracked files
