# v1.5.0_beta_2 Roadmap Update Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 更新 `docs/PropFirmPilot_v1.5.0_road_map.md`，加入 `v1.5.0_beta_2` 修復閘與 stable 的 bounded capital utilization uplift 定位。

**Architecture:** 只改 roadmap 文件，不碰程式碼。文件會先定義 `beta_2` 的 repair scope，再把 `資金使用率偏低` 明確改掛到 `v1.5.0_stable`，同時保留 `1.5.6` 的完整 capital efficiency v1 波段定位。

**Tech Stack:** Markdown, git diff review

---

### Task 1: Update Roadmap Version Framing

**Files:**
- Modify: `docs/PropFirmPilot_v1.5.0_road_map.md`

**Step 1: Update top metadata**

- 將更新日期改為 `2026-03-17`
- 在當前主線狀態加入 `v1.5.0_beta_2` 的角色說明

**Step 2: Add `v1.5.0_beta_2` release lane**

- 在波段總覽加入 `v1.5.0_beta_2`
- 新增 repair gate 詳細章節

**Step 3: Reclassify low capital utilization**

- 明確標記其不屬於 `beta_2`
- 改掛到 `v1.5.0_stable` 的 bounded optimization

**Step 4: Review diff**

Run: `git diff -- docs/PropFirmPilot_v1.5.0_road_map.md`
Expected: diff 只包含 roadmap 版本定位、波段與修復清單更新

### Task 2: Validate Internal Consistency

**Files:**
- Modify: `docs/PropFirmPilot_v1.5.0_road_map.md`

**Step 1: Check section consistency**

- `beta_2` 與 `stable` 沒有 scope overlap
- `1.5.6` 仍代表 full capital efficiency v1，而不是 stable 的 bounded uplift

**Step 2: Check conclusion consistency**

- 一句話總結要能反映 `beta_2 -> stable -> 1.5.x` 的節奏

**Step 3: Final review**

Run: `Get-Content docs/PropFirmPilot_v1.5.0_road_map.md`
Expected: 文件可直接作為單一入口 roadmap 使用
