# v1.5.0 Stable Docs Refresh Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 更新 stable 入口文檔，讓 roadmap 反映目前 `main` 的 stable 基線，並新增一份完整解說交易生命週期的 stable report。

**Architecture:** 這次工作只調整文檔，不變更程式行為。roadmap 會聚焦在版本狀態與邊界；stable report 會用交易生命週期為主線，從 scanner、scheduler、tactical、execution、position monitor 到 close reconciliation 串成完整說明，並明確標示 `TradingAgents` 預設關閉的 stable 行為。

**Tech Stack:** Markdown、git、現有 Python runtime source files、外部 scanner 中文 roadmap 參考文檔

---

### Task 1: 固定文檔基線與來源

**Files:**
- Modify: `docs/PropFirmPilot_v1.5.0_road_map.md`
- Create: `docs/PropFirmPilot_v1.5.0_stable_Report.md`
- Reference: `src/config.py`
- Reference: `src/main.py`
- Reference: `src/scheduler/scheduler.py`
- Reference: `src/signal/scanner_bridge.py`
- Reference: `src/execution/engine.py`
- Reference: `src/execution/broker_factory.py`
- Reference: `src/decision/close_control_plane.py`
- Reference: `src/decision/close_reconciler.py`
- Reference: `src/decision/tactical_validator.py`
- Reference: `src/compliance/prop_firm_guard.py`
- Reference: `src/monitor/alert_service.py`
- Reference: `src/monitor/trade_journal.py`

**Step 1: 核對 stable 文檔必須反映的事實**

需要覆蓋：

- broker-neutral startup 與 `tradelocker` / `matchtrader` backend
- `TradingAgents` 預設關閉與 env override
- stable 預設 entry funnel 為 `scanner_tactical`
- scanner bundle contract / target-date fail-closed
- tactical gate / tactical exit / close reconciliation
- monitoring / journal / alert fallback

**Step 2: 確認 roadmap 與 report 的分工**

Roadmap 只講：

- stable 主線已完成
- stable 尚未完成
- stable 之後優先方向

Report 才講：

- 系統角色
- 交易生命週期
- 模組責任邊界

**Step 3: Commit**

```bash
git add docs/plans/2026-03-23-v1.5.0-stable-docs-refresh-design.md docs/plans/2026-03-23-v1.5.0-stable-docs-refresh.md
git commit -m "docs: add stable docs refresh design and plan"
```

### Task 2: 重寫 roadmap 的 stable 入口敘事

**Files:**
- Modify: `docs/PropFirmPilot_v1.5.0_road_map.md`

**Step 1: 寫出新的開頭定位**

用 markdown 重寫文件前段，明確說：

- 這是 `v1.5.0_stable` 的主線入口
- preview 已是歷史來源，不再是主敘事
- 讀者可在此看目前主線基線與缺口

**Step 2: 重整「已完成」章節**

用目前 `main` 已落地的 stable 事實重寫：

- broker-neutral runtime
- TradeLocker-first E8 Signature path
- `TradingAgents` default-off
- scanner contract
- market-data continuity fallback
- close control plane
- monitoring / alert fallback

**Step 3: 收斂「未完成」章節**

只保留真正尚未 closure 的 stable 項目：

- worst-case natural-SL 組合風險 guard
- exposure / portfolio budget v1
- trade-memory v1
- multi-day stable acceptance

**Step 4: 收短 stable 後波段**

將舊版長篇 patch-version 敘事壓縮成較短的後續優先方向。

**Step 5: Run review**

Run: `rg -n "preview|preview_2|TradingAgents|tradelocker|matchtrader|scanner_tactical|stable" docs/PropFirmPilot_v1.5.0_road_map.md`

Expected:

- `preview` 只出現在必要背景
- `TradingAgents` 預設關閉有被明寫
- `tradelocker` / `matchtrader` / `scanner_tactical` / `stable` 都能找到對應敘事

**Step 6: Commit**

```bash
git add docs/PropFirmPilot_v1.5.0_road_map.md
git commit -m "docs: refresh v1.5.0 stable roadmap"
```

### Task 3: 撰寫 stable report 的交易生命週期主線

**Files:**
- Create: `docs/PropFirmPilot_v1.5.0_stable_Report.md`

**Step 1: 建立文件骨架**

寫出以下章節：

- 文件定位
- 一頁式系統總覽
- 啟動與系統基線
- 一筆新交易怎樣開始
- 決策層怎樣工作
- tactical gate 怎樣決定可不可以進場
- 交易怎樣真正送到 broker
- 持倉期間系統怎樣管理風險
- 一筆交易怎樣結束
- 各功能模組在整條鏈中的角色
- 當前 stable 邊界與未完成項

**Step 2: 寫預設 stable 路徑**

必須直寫：

- `qlib market scanner -> scheduler -> tactical -> execution -> position monitor -> close control / reconciliation -> journal / alert / summary`
- `TradingAgents` 預設關閉
- 開啟 `TRADINGAGENTS_ENABLED` 才進 LLM confirm/veto path

**Step 3: 按生命週期補齊關鍵細節**

至少要覆蓋：

- scanner signals 輸出與 contract validation
- intent creation 前的各種 gate
- scanner-side direct routing
- tactical hard/soft gates 與 retry semantics
- compliance / Best Day / sizing / slippage / broker execution
- tactical exit / position monitor / best day close / reevaluation boundary
- close reconciliation / final close reason / journal / alerts

**Step 4: Scanner 語氣對齊外部 roadmap**

scanner 章節要採 contract / upstream dependency 語氣，不寫成 alpha 宣傳。

**Step 5: Run review**

Run: `rg -n "TradingAgents|TRADINGAGENTS_ENABLED|scanner_tactical|scanner_llm_tactical|tactical|close|reconcile|tradelocker|matchtrader" docs/PropFirmPilot_v1.5.0_stable_Report.md`

Expected:

- `TradingAgents` 預設關閉與 opt-in 行為有清楚描述
- 交易流程各關鍵節點都有對應章節

**Step 6: Commit**

```bash
git add docs/PropFirmPilot_v1.5.0_stable_Report.md
git commit -m "docs: add v1.5.0 stable system report"
```

### Task 4: 交叉檢查與收尾

**Files:**
- Modify: `docs/PropFirmPilot_v1.5.0_road_map.md`
- Modify: `docs/PropFirmPilot_v1.5.0_stable_Report.md`

**Step 1: 比對兩份文件的邊界**

確認：

- roadmap 不展開生命週期細節
- report 不重複整份 roadmap 的版本規劃

**Step 2: 驗證引用與檔案存在**

Run: `rg -n "src/config.py|src/main.py|src/scheduler/scheduler.py|qlib_market_scanner" docs/PropFirmPilot_v1.5.0_road_map.md docs/PropFirmPilot_v1.5.0_stable_Report.md`

Expected:

- 關鍵實作來源與 scanner 參考有明確落點

**Step 3: 檢查 git diff**

Run: `git diff -- docs/PropFirmPilot_v1.5.0_road_map.md docs/PropFirmPilot_v1.5.0_stable_Report.md docs/plans/2026-03-23-v1.5.0-stable-docs-refresh-design.md docs/plans/2026-03-23-v1.5.0-stable-docs-refresh.md`

Expected:

- 只包含這次文檔更新與 planning 文檔

**Step 4: Final commit**

```bash
git add docs/PropFirmPilot_v1.5.0_road_map.md docs/PropFirmPilot_v1.5.0_stable_Report.md
git commit -m "docs: document v1.5.0 stable runtime flow"
```
