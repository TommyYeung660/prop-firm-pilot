"""Shared test-only constants for scanner contract expectations."""

ACTIVE_LABEL_VERSION = "cost_aware_net_return_sign_v1"

# Backward compat: v1/v2 bundles may still use the old label version
LEGACY_LABEL_VERSION = "binary_forward_return_sign_v1"
